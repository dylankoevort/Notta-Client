import webService from "./WebService";
import {
  getNotes,
  getNote,
  addNote,
  updateNote,
  deleteNote,
} from "./NotesGateway";

export { webService };
export { getNotes, getNote, addNote, updateNote, deleteNote };
